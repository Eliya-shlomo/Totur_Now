import React from 'react';
import ReactDOM from 'react-dom/client';

import VideoDemoPage from './pages/video/VideoDemoPage';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VideoDemoPage />
  </React.StrictMode>,
);
